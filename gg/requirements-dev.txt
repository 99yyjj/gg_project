# 개발/테스트 전용 의존성
# 설치: pip install -r requirements-dev.txt

pytest>=8.0
pytest-asyncio>=0.23
aiosqlite>=0.20      # 테스트용 인메모리 SQLite async 드라이버
# httpx는 requirements.txt에 이미 포함 (MockTransport / ASGITransport 사용)
